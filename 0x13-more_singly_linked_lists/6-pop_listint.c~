#include "lists.h"
/**
 * @brief 
 * 
 * 
 */
int pop_listint(listint_t **head)
{
        listint_t *delete = *head;
    int res = delete->n;
    if (*head == NULL)
    {
return (-1);
    }

 (*head) = (*head)->next;

free (delete);
return (res);
}