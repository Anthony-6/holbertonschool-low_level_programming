#include "main.h"
#include <string.h>

/**
 *
 *
 *
 *
 */
void reverse_array(int *a, int n)
{
	int h;
	int i = 0;
	while (i < n / 2)
	{
		h = a[i];
		a[i] = a[n - i - 1];
		a[n - i - 1] = h;
		i++;
	}
}
