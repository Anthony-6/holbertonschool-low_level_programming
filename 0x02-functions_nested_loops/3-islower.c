#include <ctype.h>

/**
 * islower - check if a character is lower than an other one
 *
 * Return - 1 if it's lower, 0 if not
 */
int _islower(int (c))

{
	if (islower (c))
	return (1);
	else
		return (0);
}
